# PPTX Spec — Deferred

Reserved per [`prompt.md`](../../prompt.md). Will be authored in the
follow-up PPTX session and will mirror the structure of `spec/docx/`.

The shared specs in [`../shared/`](../shared) already cover PPTX-applicable
infrastructure (command bus, OOXML utils, agent API, plugin system).
